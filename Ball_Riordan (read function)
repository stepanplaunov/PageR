def ReadGraph(a):
    f = open('Ball_Riordan\Ball-Riordan' + str(a) + '.txt', 'r')
    Graph = np.zeros(1000**2)
    V = f.read(1)
    g = f.read()
    g = g.split()
    for x in range(len(g) - 1):
        Graph[x] = float(g[x + 1])
    return Graph.reshape(1000,1000)
